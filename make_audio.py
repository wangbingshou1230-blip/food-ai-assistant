import asyncio
import edge_tts

# ================= 🎙️ 配音工作室设置 =================

# 1. 在下面把你想让 AI 读的文案粘贴进去
TEXT_TO_READ = """
你好，我是食品硕士 Master。
听说大家最近很担心预制菜的安全问题？
其实，只要符合国标，预制菜并没有那么可怕。
今天我们就利用大数据，来扒一扒预制菜背后的真相。
"""

# 2. 选择声音 (解开注释可切换)
# VOICE = "zh-CN-XiaoxiaoNeural"  # 👩 晓晓 (温柔知性女声，适合科普)
VOICE = "zh-CN-YunxiNeural"     # 👨 云希 (清亮阳光男声，适合快节奏视频)
# VOICE = "zh-CN-YunjianNeural"   # 👨 云健 (成熟稳重男声，适合新闻播报)

OUTPUT_FILE = "配音.mp3"

# ================= ⚙️ 核心逻辑 (不用改) =================
async def generate_audio():
    print(f"🎙️ 正在让 {VOICE} 录制音频中...")
    communicate = edge_tts.Communicate(TEXT_TO_READ, VOICE)
    await communicate.save(OUTPUT_FILE)
    print(f"✅ 录制完成！文件已保存为: {OUTPUT_FILE}")
    print("👉 请在左侧文件列表找到它，右键 -> 'Reveal in File Explorer' 打开播放")

if __name__ == "__main__":
    asyncio.run(generate_audio())